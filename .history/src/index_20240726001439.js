import { process_params } from "express/lib/router";
import mongoose from "mongoose";

(async () => {
   try {
    mongoose.connect(`${process.}`)
   } catch (error) {
    console.log(error)
   }

})()